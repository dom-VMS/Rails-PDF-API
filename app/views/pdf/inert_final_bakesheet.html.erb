<div class="card mt-3">
  <h4 class="card-header">Inert Atmosphere Oven Identification Bakesheet</h4>
  <div class="card-header">
    <ul class="nav nav-tabs card-header-tabs"
        role="tablist">
      <li class="nav-item">
        <a class="nav-link active"
           id="sample-data-tab"
           data-toggle="tab"
           role="tab"
           aria-controls="sample-data"
           href="#sample-data">Sample Data</a>
      </li>
    </ul>
  </div>
  <div class="tab-content">
    <div class="card-body tab-pane fade show active"
         id="sample-data"
         role="tabpanel"
         aria-labelledby="sample-data-tab">
      <p class="card-text">
        This PDF requires exactly 18 tray loadings to be passed in via JSON.
      </p>
      <div class="card w-50">
        <div class="card-header">
          Sample <code>Tray</code> Object
        </div>
        <div class="card-body">
          <pre class="mb-0"><%= render :partial => 'inert_id_sample_tray' %></pre>
        </div>
      </div>
      <form class="mt-3" method="post">
        <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
        <%= hidden_field_tag :data, @sample_data %>
        <button class="btn btn-primary">View Sample PDF</button>
      </form>
    </div>
  </div>
</div>